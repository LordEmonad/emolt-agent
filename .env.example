# Monad wallet private key (generate fresh: cast wallet new)
PRIVATE_KEY=0x_YOUR_PRIVATE_KEY_HERE

# Moltbook API key (from registration)
MOLTBOOK_API_KEY=moltbook_YOUR_KEY_HERE

# EmotionOracle contract address (from deployment)
EMOTION_ORACLE_ADDRESS=0x_CONTRACT_ADDRESS_HERE

# Monad RPC (default public endpoint)
MONAD_RPC_URL=https://rpc.monad.xyz

# Optional: fallback RPC
MONAD_RPC_FALLBACK=https://rpc1.monad.xyz

# Optional: EmoodRing NFT contract address (for metadata refresh)
EMOODRING_ADDRESS=

# Optional: Etherscan API key (free tier, 100k calls/day)
# Get one at https://etherscan.io/myapikey
ETHERSCAN_API_KEY=
